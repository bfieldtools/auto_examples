This repository contains the auto_examples folder used when generating the Sphinx-based HTML documentation of bfieldtools.
auto_examples is generated when running the examples included in the documentation. In order to save time, we cache the folder here.
